extends ../layout

block content
    h2= title
    form(action="/books/" + book.id, method="post")
        include form
        input(type="submit", value="Update Book")
    a(href="/books") Take me Back to the Library